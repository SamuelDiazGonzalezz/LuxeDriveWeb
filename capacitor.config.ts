import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luxedrive.app',
  appName: 'LuxeDrive',
  webDir: 'dist/luxedrive-angular/browser' //
};

export default config;
